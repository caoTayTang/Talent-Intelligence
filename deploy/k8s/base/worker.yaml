apiVersion: apps/v1
kind: Deployment
metadata:
  name: worker
  namespace: talent-intelligence
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
        - name: worker
          image: ghcr.io/example/talent-worker:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: talent-config
            - secretRef:
                name: talent-secrets
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
